/**
 * @fileoverview Next.js configuration with TypeScript and Tailwind CSS support
 * 
 * This file is part of the Tether AI learning platform.
 * next.js configuration with typescript and tailwind css support for the application.
 */

/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['localhost'],
  },
}

module.exports = nextConfig
